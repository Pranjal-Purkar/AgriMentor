spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Efarming
#    url: jdbc:mariadb://localhost:3306/Efarming
    username: root
    password: password 
    driver-class-name: com.mysql.cj.jdbc.Driver
#    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create        # auto update schema
    show-sql: true              # show SQL queries in console
    properties:
      hibernate:
        format_sql: true        # ✅ format SQL nicely
        use_sql_comments: true  # show helpful comments in SQL
        dialect: org.hibernate.dialect.MySQL8Dialect
#        dialect: org.hibernate.dialect.MariaDBDialect

#  sql:
#    init:
#      mode: always
#      schema-locations: classpath:schema.sql
#      data-locations: classpath:data.sql

server:
  port: 8080
  address: 0.0.0.0

jwt:
  secret-key: "fsdmfdlsmfdfndsfkndsfndsnfdsnfdsnfjdfdsnfkjdf"